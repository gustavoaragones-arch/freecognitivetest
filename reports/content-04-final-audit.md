# CONTENT-04G — Final Regression Audit

## Objective

Restore `validate-arch02.mjs` to full PASS without touching CONTENT-03's content work. Confirmed achieved.

## Certification results

| Check | Before CONTENT-04 | After CONTENT-04 |
|---|---|---|
| `scripts/validate-arch02.mjs` | PARTIAL (`stub_pages_indexed: 11`) | **PASS — 10/10 checks** |
| `stub_pages_indexed` | 11 | **0** |
| `broken_localized_switches` | 0 | 0 |
| `canonical_issues_audit` | 0 | 0 |
| `hreflang_issues_audit` | 0 (didn't catch the manually-found defect) | 0 (defect now actually fixed, not just unflagged) |
| `scripts/validate-auth01.mjs` | PASS | PASS |
| `scripts/validate-content02-gate.mjs` | PASS (0 new rows) | PASS (0 new rows) |
| Semantic eligibility sweep | 0/510 | 0/510 |
| Redirect chains | — | 0/11 |
| Redirect loops | — | 0/11 |
| Redirect destinations exist | — | 11/11 |

## What was done, by subphase

- **04A** — Compiled the exact evidence record for all 11 redirected URLs (source, destination, ES/FR siblings, inbound link count) directly from `redirects.json`, `path-mirror-rows.json`, and the site's own crawled link graph. No inferred URLs.
- **04B** — Found the authoritative mechanism (`scripts/build-sitemap-index.mjs`, which already had a `SITEMAP_EXCLUDE` precedent for the site's pre-existing legacy redirects). Added the 11 URLs to it. Removed their `<url>` entries from `sitemap-programmatic-1.xml`/`-2.xml` (the two files that mechanism treats as fixed input, not output) via exact, verified single-block removal. Ran the mechanism. Result: 11 URLs removed, the 2 new destination URLs picked up automatically by the same run (via its walk-all-HTML-files fallback), 0 unrelated URLs lost, 0 duplicates, all XML valid — verified by comparing sorted `<loc>` sets before/after, not just line counts.
- **04C** — Found that `path-mirror-rows.json` (generated by `scripts/build-path-mirror-rows.mjs` from `assets/data/seo-pages-manifest.json`) is what `validate-arch02.mjs`'s hreflang check actually iterates over. Removed the 11 EN entries from the manifest (keeping their ES/FR entries), regenerated the mirror-rows file with the existing safe script (170 → 159 rows), then removed the stale `hreflang="en"` tag and repointed `hreflang="x-default"` to self-reference on all 22 ES/FR sibling files. Considered and rejected repointing `hreflang="en"` at the new consolidated page instead — that would require one EN URL to reciprocally declare hreflang alternates to 7 (or 4) different, unrelated ES/FR URLs, which both violates the 1:1 hreflang model and would have been caught by the audit's own reciprocity check.
- **04D** — Verified all 11 redirects directly against `redirects.json`: 0 chains (no destination is itself a redirect source), 0 loops, all 11 destination files exist on disk and are indexable (no noindex, self-canonical).
- **04E** — Found and fixed internal `<a href>` links to the 11 old URLs in the 5 primary hub/index pages (`ai-index.html`, `programmatic/index.html`, `brain-exercises/all.html`, `index.html`, `en/index.html` — 39 links total). Deliberately left the ~75 peer programmatic pages' auto-generated "related articles" cross-links untouched, since fixing those would be the explicitly forbidden site-wide link rewrite; they still resolve correctly through the verified redirects.
- **04F** — Full certification run, all green (table above).
- **04G** — This document.

## Regression check against the CONTENT-03 completion state

| Preserved item | Verified |
|---|---|
| 5 editorial pilot pages (`memory-test-for-caregivers`, `-post-stroke`, `-parkinsons`, `early-signs-of-dementia`, `medication-side-effects-memory`) | Unchanged — `.unique-content` sections present and intact; not touched by any CONTENT-04 script (none operate on these paths) |
| 2 consolidated resources (`how-to-use-memory-aids/`, `brain-exercises/attention-and-focus/`) | Unchanged content; only referenced (not edited) by the sitemap/hub-link fixes |
| 323 semantic (ADHD/bilingual) corrections | Spot-checked intact; independent semantic-eligibility sweep still reports 0/510 |
| `assets/data/programmatic-seeds.json` | Byte-identical (`git diff` empty) |
| `assets/data/programmatic-frozen-manifest.json` | Unchanged, still 170 slugs |
| `scripts/lib/content-value-gate.mjs` | Not modified — no criterion touched, no version bump |
| CONTENT-02 gate | Still reports 0 new seed rows |
| New programmatic pages created | 0 |
| New redirects created | 0 (still exactly 42 total rules — the pre-existing 31 + the same 11 from CONTENT-03D) |
| Pages deleted | 0 |
| Sitemap URLs lost outside the 11 known redirected sources | 0 (verified by full sorted-set diff, not just counts) |
| Canonical tags changed on unrelated pages | 0 |
| hreflang changed on unrelated pages | 0 (only the 22 specifically-identified sibling files) |

## Files changed this phase (exact list)

**Source mechanisms (2):**
- `scripts/build-sitemap-index.mjs` — `SITEMAP_EXCLUDE` +11 entries
- (no change to `scripts/build-path-mirror-rows.mjs` itself — it already correctly derives from the manifest; only its input and output data changed)

**Data files (2):**
- `assets/data/seo-pages-manifest.json` — 11 EN entries removed (510 → 499)
- `assets/data/path-mirror-rows.json` — regenerated (170 → 159 rows)

**Sitemap XML (6):**
- `sitemap-programmatic-1.xml` (−4 URLs)
- `sitemap-programmatic-2.xml` (−7 URLs)
- `sitemap-main.xml` (+2 URLs)
- `sitemap-supplemental-en.xml`, `sitemap-es.xml`, `sitemap-fr.xml`, `sitemap.xml` (lastmod timestamp refresh only, from running the existing generator — no URL-set change)

**ES/FR sibling pages (22):** the pages listed in `reports/content-04-hreflang-audit.json`

**Hub/index pages (5):** `ai-index.html`, `programmatic/index.html`, `brain-exercises/all.html`, `index.html`, `en/index.html`

**Reports (4, new):** `content-04-sitemap-audit.json`, `content-04-hreflang-audit.json`, `content-04-internal-links.json`, `content-04-final-audit.md` (this file)

**Total: 37 substantive files + 4 report files = 41.** No file outside this list was touched this phase.
